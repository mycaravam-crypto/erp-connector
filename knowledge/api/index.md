# API

How to call the connector's API — authentication and the endpoints for triggering or previewing
an export.

* [Authentication](authentication.md) - JWT Bearer auth protecting all routes; BCrypt user store; opt-in API-key scheme for machine callers
* [On-Demand Run](on-demand-run.md) - API-triggered full pipeline run, saved-preset trigger, and read-only preview
* [Export Definition API](export-definition-api.md) - CRUD, manual trigger, test, preview, and run history for saved, named export definitions (Phase 14)
