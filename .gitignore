# .NET build output
bin/
obj/
*.user
*.suo
.vs/
.vscode/

# SQLite runtime databases (generated at startup — not source artefacts)
*.db
*.db-shm
*.db-wal

# Staging folder (export files written at runtime)
src/Connector.Api/staging/

# Test result artefacts
TestResults/
*.trx
*.coverage
coverage*/

# NuGet
*.nupkg
.nuget/

# Node / Vue frontend (future)
node_modules/
dist/
.env
.env.local
.env.*.local
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# Claude Code local state (worktrees, settings, memory) — except the
# team-wide project config and project skills, which are intentionally tracked.
.claude/*
!.claude/settings.json
!.claude/skills/
!.claude/skills/**

# Internal development log (session notes — not for public distribution)
knowledge/log.md
